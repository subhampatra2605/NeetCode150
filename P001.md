1. Contains Duplicate
✅ Problem

Check if any number appears more than once in the array.

📥 Input

nums = [1,2,3,1]
📤 Output → true

💀 Brute Force

Compare every pair.

TC: O(n²)

SC: O(1)

🚀 Optimal (HashSet)

Insert each element; if already exists → duplicate.

TC: O(n)

SC: O(n)

✅ Java Code
class Solution {
    public boolean containsDuplicate(int[] nums) {
        HashSet<Integer> set = new HashSet<>();
        for (int n : nums)
            if (!set.add(n)) return true;
        return false;
    }
}
